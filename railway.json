{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "echo '=== FULL ENVIRONMENT DEBUG ===' && env | sort && echo '=== MYSQL/DATABASE VARS ===' && env | grep -E '(MYSQL|DATABASE)' && echo '=== END DEBUG ===' && npm run db:deploy && npm start"
    }
}
