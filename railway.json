{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx prisma generate && node railway-deploy-simple.js"
  },
  "deploy": {
    "startCommand": "node server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
