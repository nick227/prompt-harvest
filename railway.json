{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deploy": {
        "startCommand": "echo 'Environment check:' && env | grep -E '(MYSQL|DATABASE)' && npm run db:deploy && npm start"
    }
}
