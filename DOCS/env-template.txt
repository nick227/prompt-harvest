# Database Configuration
MYSQL_URL="file:./db/database.db"

# JWT Secret for authentication tokens
JWT_SECRET="your-super-secret-jwt-key-here"

# Server Configuration
PORT=3200
NODE_ENV=development

# Frontend URL for email links
FRONTEND_URL=http://localhost:3200

# Email Configuration (Resend)
RESEND_API_KEY=your_resend_api_key_here
FROM_EMAIL=noreply@yourdomain.com

# Optional: Override default from email for specific environments
# FROM_EMAIL=support@imageharvest.com

# Security Configuration
# CORS_ORIGIN=http://localhost:3200
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX_REQUESTS=100

# Production Email Configuration Examples:
# For production, you might want to use a custom domain:
# FROM_EMAIL=noreply@yourdomain.com
# FRONTEND_URL=https://yourdomain.com

# Development vs Production Notes:
# - Development: FROM_EMAIL can be any valid email format for testing
# - Production: FROM_EMAIL should match your verified domain in Resend
# - Make sure to verify your domain in Resend dashboard for production use
