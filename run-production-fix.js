#!/usr/bin/env node

/**
 * Run Production Model Fix
 * 
 * This script can be run on Railway to fix the model configurations.
 * It imports and runs the fix script.
 */

import './scripts/fix-production-model-configs.js';
